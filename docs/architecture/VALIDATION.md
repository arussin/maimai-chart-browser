# Integrated implementation validation

> Historical initial candidate. See [the production revision](REVISION.md) for the current architecture and acceptance results.

Validation date: 2026-09-22. This record covers the architecture refactor, sustainable coverage, multilingual SEO, first-party counts and owner reporting as one implementation. It distinguishes source review, deterministic tests, bounded real-source preparation and production verification. A passing local test is not a claim that its scenario was exercised against a production account or a private player report.

**Current disposition:** implemented local review candidate; combined launch blocked. The complete immutable public corpus needs **29,049 files**, exceeding the enforced 20,000-file limit by **9,049**. No deployable release manifest or `ready.json` was produced for this over-capacity candidate. Production activation, account configuration and final deployed verification remain owner-controlled gates.

## Scope and precedence

The source record is the recovered [coverage handoff](handoffs/coverage-original.md), its [accepted amendments](handoffs/coverage-amendments.md), the recovered [SEO/usage handoff](handoffs/seo-usage-original.md), and its [accepted amendments](handoffs/seo-usage-amendments.md). They remain preserved; historical assertions inside them are not current-state evidence.

| Earlier statement | Accepted integrated decision |
| --- | --- |
| Pending Maishift launch/checkpoint branches | Registry starts at merged PR #42, `67e377dc2a5a095e2fe05b331235fb679895a4bd`. Report starts at PR #21, `c4992ce14e3b5a0821e96a801c8cf925f60f41cf`. |
| Coverage amendment says not to implement separate SEO/logging | The later explicit integrated implementation request supersedes this scope exclusion. Coverage, SEO, counts and reporting are included together; rollout/account restrictions remain. |
| Original Analytics Engine proposal | Dedicated D1 daily totals and a coverage ledger, with an owner-run report command. No event-history table or visitor/session identifiers. |
| Unprefixed/optional song-index routes | Four canonical folders (`en`, `ja`, `ko`, `zh-hans`), one song page per canonical song identity, all STD/DX difficulties. Unprefixed routes redirect to English; optional song index omitted. |
| Estimated detail deduplication would fit the hosting ceiling | Preserve every verified published startup/detail URL and add the shared format. The measured full corpus exceeds capacity; do not retire immutable URLs or bypass the guard. |
| Stack migration permitted | Pinned build-only TypeScript modules with existing script/global/DOM facades. No framework rewrite, runtime SSR, Python-install Node dependency or report protocol migration. |
| Existing visual experience preserved | Existing browser/report layouts and inline chart expansion remain. Authorized additions are separate song-page links, new localized song/version documents and their regional control, privacy disclosure changes, and intentional coverage/title-state corrections. |

Canonical source checkouts remain at `C:/Dev/maimai/maimai-chart-browser-registry` and the existing report location. Implementation uses the explicitly authorized linked checkouts `C:/Dev/worktrees/maimai-architecture-registry` and `C:/Dev/worktrees/maimai-architecture-report`. Read-only final checks found the canonical registry still at `67e377d` with its 12 pre-existing dirty entries, canonical report at `776e0df` with its nine dirty entries, and remote registry main at `67e377d`. These are preserved checkout observations, separate from implementation branch commits. No canonical identity, private player history, protected Git storage, ACL, credential, automation or retained production artifact was rewritten by the refactor.

Copied unpublished development-layout/retention work is excluded from product delivery: `tools/Workspace-Retention.ps1`, `tools/Test-WorkspaceRetention.ps1` and unrelated retention documentation are not part of the architecture changes. Their canonical originals are preserved. The development wrapper retains only the scoped offline-cache support needed for validation. Environments, dependencies, fixtures, screenshots and review bundles remain in DevCache.

## Requirement-to-evidence matrix

“Local” below means the implementation and listed tests are present. It does not replace the production gates at the end of this record.

| Requirement | Implementation / tests | Evidence and limits |
| --- | --- | --- |
| Separate preparation from presentation | `catalog_preparation.py`, `PreparedCatalog`, `render_prepared_review`; report `PreparedReport`/`PartyContext`; installable parser/count/identity services. Catalog HTML is no longer parsed back into JSON. | Registry Python suite; report Python suite; four catalog/pattern hash fixtures; 18 report HTML byte-identity modes. No UI rewrite. |
| Stable library/report contract | `docs/PUBLIC_CONTRACTS.md`, contract exporter/verifier, report vendor preparation and offline consumption; CLI facades retained. | Git-object/dirty-tree/traversal/tamper rejection and offline package tests. Final committed contract identity must be checked at delivery; local source tests do not manufacture a future commit SHA. |
| Detached typed browser domains | `web/src/player-session.ts`, `catalog-query.ts`, pinned build/asset manifest; existing adapters retain their APIs. | Eight typed-domain tests; Python asset/package assertions; browser import/race/region cases. Existing script sequencing and offline report boundaries remain. |
| Durable identities, title state and enrichment | Existing six-table registry, song-owned artwork/history, chart provider outcomes, explicit title classifications and scoped evidence. | `test_registry.py`, `test_sustainable_coverage.py`, package/roundtrip tests. Real 7,251 chart IDs, 1,694 song IDs, profile hashes and analysis remain unchanged. One reviewed U+3000 intentional blank keeps its raw title and credit. |
| Conservative provider reconciliation | Immutable same-revision captures, last-known-good relationships, scoped credit/blank-title rules, `policy_exact`, explicit usability/freshness outcomes. | Deterministic ambiguity/conflict/schema/reassignment fixtures. Real capture retained all 6,796 mappings; **455 gaps remain**. No claim of new real mappings. Maishift evidence grants no Kamaitachi matching authority. |
| Stored unknown PB becomes visible when mapping arrives | Provider-aware lookup; accepted `policy_exact` projection; player storage unchanged. | `progressive.spec.js`: synthetic N→N+1 reload reveals retained PB without reimport; stored bytes/revision unchanged; explicitly pinned N still has no mapping. This is synthetic personal data, not a private-account canary. |
| All-song artwork waterfall | Metadata-only/JP/INTL/historical songs, verified retained selections, approved official/Wiki sources, identity checks, bounded decoding, hashes and provenance. | Real artwork **1,425→1,443 (+18)**; no removals; **251 gaps remain**. Unit fixtures cover wrong edition, malformed/placeholder/unsafe responses and retained-on-failure behavior. |
| Incremental preparation, replay and readiness | Common source/package/metadata-only/refresh stage; durable bounded queue, reserved oldest work, reason-specific retry/expiry; atomic progress and receipt-bound replay. | Real bound captures reproduced exactly, ordinary offline reuse retained state, registry/package roundtrips preserved public data. Synthetic tests cover overflow, 404/429/backoff, changed evidence and tampering. The real run was one bounded batch, not exhaustive acquisition. |
| Complete public release planning | Immutable `ReleasePlan`, validation before writes, fresh destination, manifest last, explicit verified `previous_public` closure and readiness binding. | `test_public_release.py`, updater/waterfall tests and final actual-corpus receipt. All **15** historical startup/detail closures byte-identical. Capacity blocks publication before a deployable manifest/readiness can be written. |
| Shared details with old-loader compatibility | Additive `catalog-index-shared-1` / `chart-details-shared-1`; accepted catalog bytes unchanged; catalog hash stays in index. | `test_catalog_loading.py`, `progressive_loader.cjs`, browser progressive tests cover checksum, catalog/source identity, chart membership, retry, deduplication and legacy loading. Historical assets remain available rather than being rewritten to the new format. |
| Complete localized static SEO | `seo.py`, persistent `permalinks.json`, canonical identity/routes, regional facts/artwork, translations, canonicals/hreflang/x-default, redirects, sitemap index and real 404. | `test_seo.py` covers five representative songs/two versions, Unicode/punctuation/duplicate titles, escaping, title corrections, identity redirects and regional state. Actual plan contains **1,694 songs, 28 versions, 6,888 localized documents**. No catalog duplication into each page or personal/report/payment sitemap routes. |
| Exact browser return and direct arrivals | `seo-navigation.js`, browser/region/personal-filter snapshot adapters, one committed navigation lifecycle. Mounted browser retained; history contains controls/public IDs, never copied scores. | Final **75/75** across Chromium desktop/mobile/320px, Firefox and WebKit: Back/Forward/results/reload; exact saved controls, locale, focus and scroll; direct/in-app version enhancement; canonical/title restoration; failed-history static fallback; explicit Open-browser chart+language, destination history and one actual collector activation. Pending focus restoration is cancelled by new input/navigation, and finite layout changes settle before saved scrolling. Back-results restores the original locale; explicit primary actions honor their generated language target. Existing usage tests cover prerender/BFCache deduplication. These remain bounded synthetic scenarios. |
| Existing and new-page visual parity | Existing DOM/CSS; scoped lightweight static foundation removed before version enhancement displays the existing app. Shared title labels in four languages. | Final **36 existing-UI screenshot comparisons**, plus **40 zero-tolerance decoded CSS comparisons +40 same-DOM return-stability comparisons** (four locales × song/version × five browser projects). All passed. Firefox separate-context glyph rasterization variation was isolated with identical computed styles; the final test compares lean→aggregate→lean in the same DOM, without a tolerance or product CSS change. Supplemental [SEO language review](SEO_LANGUAGE_REVIEW.md) and translation catalog reviews are source-bound AI reviews, not native-speaker certification. |
| Explicit, finite usage actions | `web/src/usage-contract.ts`, explicit browser hooks and semantic navigation events. Covers settings, catalog/personal filters, search, chart sections, comparison/similarity, file/report/Maishift imports, personal data controls, sharing, resources and issue reporting. | Source audit plus synthetic `usage.spec.js` and client/Worker tests. Import completion follows application, not dialog closure; automatic restore/refresh suppressed. Only implemented actions are emitted; reserved enum values do not imply a corresponding UI exists. No raw state/URL/text/error or identifier is accepted. |
| Bounded privacy-preserving collector | Dedicated Worker; strict production host/path/origin/schema, whole-batch validation, 16 rows/4 KiB/count1–100, in-memory sparse flush, no retry, GPC/DNT, independent kills. | Real local D1 transaction/concurrency/rollback tests; malformed/privacy-sentinel and slow/offline browser fixtures. Optional GA remains separate; retired Cloudflare browser beacon blocked in candidate tests. Actual inherited account-level injection/logging still requires owner review. |
| Daily owner reports and truthful coverage | D1 New York daily totals with no expiry; owner-maintained complete/partial/off coverage ledger; JSON/Markdown/totals CSV/coverage CSV; UTC day boundaries and DST; local-by-default read-only reporting. | Twelve usage/report tests and actual owner CLI execution against synthetic local D1. Unknown/partial periods never become measured zero or guessed failures. No unique-user/session/funnel/retention claim. Production D1 is not provisioned or queried. |
| Browser and payload performance | Pinned baseline versus SEO-only versus SEO+usage; equal fixture bytes; five alternating cold/warm iterations at 1280×900 across three synthetic datasets; retained/enriched actual-corpus supplement separate from same-data comparison. Three-width coverage belongs to visual tests, not this timing sample. | Final source-bound run completed: 130 root records +20 deep-page groups, no resource/page errors or nonlocal attempts. Back readiness includes restored focus/scroll and two animation frames; earlier weaker-readiness Back timings are superseded. The measurements identified and verified removal of the heavy/duplicate initial stylesheet. See [PERFORMANCE.md](PERFORMANCE.md) for current measured differences, exact payloads, source binding and limitations. These are localhost/browser measurements, not field Core Web Vitals. |
| Preserve player/report/Maishift behavior | Provider/region separation, rating-only enrichment, no invented PBs/history, import-generation invalidation, rollback/Forget, pilot/main isolation, offline exports, payments/sharing. | Existing browser suites and report tests. Pilot regression checks every referenced script exists, including `player-session.js`, and excludes usage instrumentation. No real private report was accessed or resynced. |

## Test checkpoint ledger

Do not add these rows together as independent coverage: some focused tests are also included in broader suites.

| Check | Result at this checkpoint | Exact retained evidence |
| --- | --- | --- |
| Registry full Python | **582 run, OK; 7 skipped** because Windows symbolic-link creation was unavailable. Those link-specific assertions were not exercised here. | `C:/DevCache/projects/maimai-chart-browser-registry/50b28fa463dbfb9a/workspaces/20260922T105900150-8679c246/output/python-final-verbose.log` |
| Affected registry Python follow-up | **30 passed** in 4.655s on the final delivery source: release planning/public capability, SEO HTML and catalog loading. | `C:/DevCache/projects/maimai-chart-browser-registry/50b28fa463dbfb9a/workspaces/20260922T122833644-cb17083c/output/python-seo-release-final.log`. |
| Final localization/source audit | **Passed:** all 951 messages in three translated languages, review freshness, multilingual-search freshness and Acorn source-string inventory. Existing 741 pronunciation/alias review flags remain; these are not newly accepted display titles. | Final source copy `20260922T122833644-cb17083c/output/localization-final.log`; all four commands exited zero. Root Ruff lint and formatting checks also passed for all 275 Python files. |
| Report final Python | **270 run, OK; 1 skip**; owner-provided final run record. | Final source copy: `C:/DevCache/projects/maimai-session-report/adb4b9f3157f76c0/workspaces/20260922T110310518-51886d1b`. Final test output is retained in the validating owner’s execution record; it was not redirected to a separate log. The source copy above records the exact tested files. |
| Report HTML equivalence | **18 modes byte-identical**, covering complete/empty/incomplete and supported presentation options. | `C:/DevCache/projects/maimai-session-report/architecture-preparation/baseline-html-sha256.json`; comparison printed in retained execution output. |
| Report browser | **460 passed** on the report preparation checkpoint. Later final contract/vendor fixes are covered by final Python; this browser run is not relabeled as a new final-source run. | `C:/DevCache/projects/maimai-session-report/adb4b9f3157f76c0/workspaces/20260922T100851287-4590253b/tests/browser/test-results/.last-run.json` (`passed`, no failed tests). Full count is in retained execution output; no separate full-text log was saved. |
| Typed domains | **8 passed**; final build consistency/types verified by owner. | `C:/DevCache/projects/maimai-chart-browser-registry/50b28fa463dbfb9a/workspaces/20260922T104220406-719b4e11/output/typed-web-usage-final.log`; final build copy `20260922T111522161-a70db8bf`. |
| Usage/D1/report tests | **12 unit tests passed**; the actual local-D1 owner reporting command passed separately and is not counted in those 12. No remote/account access. | Synthetic local D1 reports: `C:/DevCache/projects/maimai-chart-browser-registry/50b28fa463dbfb9a/workspaces/20260922T104220406-719b4e11/usage-worker/owner-validation/` (JSON, Markdown, totals CSV and coverage CSV). Final `report-final.log` is in that directory; `output/typed-web-usage-final.log` in its parent workspace records all 8 domain + 12 usage/report tests. Local D1 state uses the established short DevCache temp path after reproducing a native failure on a deeply nested temporary path; the Worker did not change to bypass it. |
| SEO/navigation | **75/75 passed** in 47.7s, no skips: 15 scenarios × five projects (Chromium desktop/mobile/320px, Firefox and WebKit). Includes 40 strict decoded-pixel CSS comparisons and 40 return-stability comparisons. | `C:/DevCache/projects/maimai-chart-browser-registry/50b28fa463dbfb9a/workspaces/20260922T122833644-cb17083c/output/seo-browser-final.log`; separate `tests/browser/seo-test-results-final/`. |
| Existing UI visual baseline | **36 exact screenshot comparisons passed** in 20.39s (12 tests); equal baseline/candidate data avoids random fixture identity/order differences. | Baseline: `C:/DevCache/projects/maimai-chart-browser-registry/50b28fa463dbfb9a/architecture-baseline/`. Final copy `20260922T122833644-cb17083c/output/browser-visual-final-results.json` and `browser-visual-final.log`; `visual-inputs.json` records exact data and final emitted runtime hashes. Isolated `browser-visual-tests` leaves functional fixtures unchanged. |
| Main registry browser | **Initial run:** 1,182 passed, 17 failed, 5 intentional skips, 0 flaky (1,204 total). Failures were four normalize-contract cases, four unknown-genre fixture cases that modified the superseded startup reference, and nine standalone permalink requests. Corrections are implemented and the complete 343-case registry/standalone follow-up passed on final copy `20260922T111921392-a956693c`, resolving all 17 failures; the original run remains recorded as failed. SEO and isolated performance cases run separately. | `C:/DevCache/projects/maimai-chart-browser-registry/50b28fa463dbfb9a/workspaces/20260922T105905539-c135f701/output/browser-main-results.json` and `browser-main.log`. |
| Complete affected browser follow-up | **343/343 passed** in 175.12s; no failures, skips or flaky cases. Covers every configured registry and standalone project, both preferred shared and legacy unknown-genre rejection, historical normalize compatibility and standalone network quiescence. | `C:/DevCache/projects/maimai-chart-browser-registry/50b28fa463dbfb9a/workspaces/20260922T111921392-a956693c/output/browser-affected-results.json` and `browser-affected.log`. |
| Final usage/locale/standalone follow-up | **78/78 passed** in 37.72s, no failures/skips/flaky cases, on the final delivery source. A detached-row test race was corrected to scroll the persistent results container while retaining its visible SVG/accessibility assertions. | Final-copy `20260922T122833644-cb17083c/output/browser-usage-locale-final-results.json` and `browser-usage-locale-final.log`. |
| Isolated desktop capacity budgets | **2/2 passed** in 3.57s, one worker, after competing generation/browser work stopped. Includes the 7,000-chart progressive capacity fixture. | Final `20260922T122833644-cb17083c/output/browser-capacity-budget-results.json` and `browser-capacity-budget.log`. |
| Final repeated performance | **130 root records +20 deep groups completed** on final source; zero resource/page errors and nonlocal attempts. All saved-position Back checks passed. See [PERFORMANCE.md](PERFORMANCE.md) for comparative transfer/timing results, dispersion and limits; these are controlled local measurements. | `C:/DevCache/projects/maimai-chart-browser-registry/50b28fa463dbfb9a/performance/final-reviewed-source/measurements.json`, SHA-256 `db82d9e98a9f5025c17cb03fad46bffba5441db030063f361864edbbb58d590f`. Binds source `20260922T122833644-cb17083c` and `review-bundle-final-delivery`; harness SHA-256 `1fea971dd7f238e7a3567c22b2bd43bcaa31637d184f2152410663a5c4f6f1e0`. |

The five main-suite skips are three optional official Google SDK audits (`GA_SDK_PATH` absent on desktop/mobile/narrow) and two desktop-only capacity-network budget cases excluded on mobile/narrow. The desktop capacity case passed. Synthetic retained-PB N→N+1 passed all three default projects.

The full registry Python checkpoint is `20260922T105900150-8679c246`; the original broad browser/actual-release checkpoint is `20260922T105905539-c135f701`. The initial corrective validation used `20260922T111921392-a956693c`: preserve the historical in-place normalize adapter while cloning at the controller boundary; enable permalink lookup only in explicitly built public releases; exercise shared and legacy startup tampering separately. A later `seo.py` adjacent-string formatting split changes no emitted HTML bytes. Generated assets are checked into source deliberately after the pinned build, while package installations and test output remain outside Dev. Source localization/privacy reviews and installable wheel/sdist checks are separate gates; a screenshot result does not prove package completeness or privacy. Registry `test_distribution` in the final Python suite verifies source-distribution → wheel package asset bytes. Report wheel/sdist artifacts are retained at `C:/DevCache/projects/maimai-session-report/adb4b9f3157f76c0/workspaces/20260922T101005570-468cd2ee/output/packages/`; this is the packaging checkpoint, not a claim that a later commit was already packaged.

The navigation hardening checkpoints remain recorded separately. The 60-case run at `20260922T114922489-52e91810/output/seo-browser-final.log` had 47 passes and 13 failures: five ambiguous primary-action selectors, five reloaded-scroll cases, two WebKit focus cases and Firefox font-readiness under disabled scripting. `output/scroll-probe.json` measured WebKit's pre-click focus change and post-disclosure scroll anchoring. The 65-case `20260922T121011526-98c4c490/output/seo-browser-final.log` then had 60 passes and five test assertions using a different Korean word from the established reviewed catalog; the assertion now uses its existing wording. Subsequent route review found and fixed version-return metadata and explicit-primary locale/history state. The final75 covers those paths in all five projects; earlier runs are not relabeled as passing.

The final complete source snapshot is `20260922T122833644-cb17083c`. Functional fixtures reuse 2,171 byte-identical synthetic JSON files from `20260922T121011526-98c4c490`; final `build_lab` output supplies changed runtime and index revisions. `output/fixture-runtime-inputs.json` records the input, generated asset hashes, source-API equality checks and JSON-set hash `faaa51e6e458cca80df34e541286f7bf4b4ea5fbc8d22c3d092f6109000c5ace`. The final real review plan independently binds the same emitted runtime to the complete retained corpus.

## Real coverage and corpus evidence

See [COVERAGE_VALIDATION.md](COVERAGE_VALIDATION.md) for all immutable input/artifact hashes, regional denominators, added identities, provenance and orchestration follow-up. The real pass attempted 300 songs; **1,394 remain queued**. All **265 JP image requests failed TLS certificate validation**. Verification was not weakened; those failures mean acquisition was unavailable, not that images are absent. Valid retained/International selections survived. The remaining 455 mapping and 251 artwork gaps remain explicit source/review/deferred work.

The final enriched input is `C:/DevCache/projects/maimai-chart-browser-registry/50b28fa463dbfb9a/workspaces/20260922T102804355-3201db86/output/real-coverage-final/package`; sibling `registry/`, `summary.json`, coverage receipts and public-data projection are retained. This input's identity/profile/analysis preservation is independent of later browser asset changes.

The previous public release is `C:/DevCache/projects/maimai-chart-browser-registry/6c15e789d6fd4518/workspaces/player-import-release-20260922-v4/public`, with manifest SHA-256 `fefc0bc79898b7b3e80ba0e61269e32dc28af5abe6baf3f0f1d63c033d792a9b`. The final plan reads only the allowlisted verified reference closure, not arbitrary files from that folder.

Final review artifacts are under `C:/DevCache/maimai-architecture-seo/actual-candidate-20260922-1036/`:

- `compatibility-receipt-final-delivery.json`: prior manifest identity, all 15 retained startup/detail closure checks, final emitted-runtime asset hashes, source-copy path and summary. SHA-256: `ba8f8c84aadddb2ebb4f4c03ecbe9380b04c2b9394cf5cd53cd4e424b1624d38`. Independent verification confirmed all eight emitted assets match `planned-assets` and the final browser fixture, and 242 source/configuration files equal the frozen snapshot after text line-ending normalization; source API assertions verify the generated files against the frozen copy, and the final delivery receipt separately records archive identity; bundled/templated output hashes are not mislabeled as raw source hashes.
- `review-bundle-final-delivery/planned-assets/`: complete proposed assets, deliberately without `manifest.json`.
- `review-bundle-final-delivery/planned-manifest.json` and `release-plan.json`: review documents outside the assets directory.
- `build-final-delivery-plan.py` and `build-final-delivery-plan.log`: reproducible local planning call and result; 286.5 seconds including generation, full-history validation and bundle writing.

Earlier pre-correction and pre-language bundles remain as historical evidence; the final-delivery bundle above binds the final `20260922T122833644-cb17083c` source snapshot and its runtime hashes.

The plan retains **16 catalog releases**, **15,370 legacy index/detail assets**, and all existing catalog bytes. The largest file is 25,915,247 bytes, below the separate 25 MiB guard. **29,049 total files** still exceed the count guard; neither old startup URLs nor history were removed to manufacture acceptance. This bundle is not an upload directory and must not be converted into one by moving its review manifest.

## Remaining delivery and combined launch gates

| Gate | Current state / required owner action |
| --- | --- |
| Complete local regression ledger | Final navigation (75), visual comparisons (36 existing UI, 40 CSS and 40 return stability), affected Python (30), 951 localization/source checks and 78 usage/locale/standalone cases passed. The earlier complete 343-case registry/standalone follow-up resolved all 17 original broad-suite failures. Final actual plan and both serial capacity budgets passed their validation steps; the plan correctly remains nondeployable for capacity. Final comparative timing (130 root records and 20 deep groups) also completed with no resource/page errors or nonlocal attempts. No local regression checks remain pending in this ledger. Packaging, scoped commit review and exact registry/report contract identity remain reconciled at delivery. Report commit `5a1a7b8bbc16246a65f25c3d4abbd2580a389f94` contains exactly its ten task files; unrelated copied helpers remain excluded. |
| Hosting capacity | **Blocked:** 9,049 files over the current guard. Requires an explicit reviewed capacity/hosting decision; no paid tier, new backend, immutable URL retirement or relaxed guard is assumed. |
| Readiness | **Absent by design for the blocked candidate.** A future capacity-valid public build must bind current implementation/config/policies, selected state/assets/coverage receipts and exact previous publication, then write `ready.json` last. Coverage-package/review artifacts are not readiness substitutes. |
| Collector production configuration | **Disabled local-only placeholder.** `usage-worker/wrangler.jsonc` has `USAGE_ENABLED=false`, all-zero D1 ID, no production route, Workers.dev/previews disabled and observability/logpush disabled. It must not be deployed as-is. Client requests are also limited to the exact production origin and subject to GPC/DNT/client kill; synthetic hosted browser contexts do not activate a real collector. |
| Account, cost and privacy review | Owner must verify actual D1 binding/schema, quotas/costs, exact `/__usage` route and malformed-suffix rejection, account-level beacon injection, inherited invocation/payload/tracing/export sinks, privacy basis, disclosures and objection mechanism. Local configuration cannot prove inherited account state. If disclosure plus GPC/DNT is insufficient, hold the combined launch and resolve the concrete conflict with Adam. |
| Protected controls | New reporting credentials, account permissions or production bindings require: **“This crosses the protected security boundary and needs the security-sensitive / privileged workflow.”** This implementation did not change those controls. |
| Combined publication and live proof | SEO, active first-party counts and usable owner reports launch together only after explicit owner authorization. Verify deployed HTML/requests, accepted nonpersonal canary counters and report output, plus real immutable URLs and rollback. No reduced-scope SEO-only release is silently approved. |
| Rollback and history | Keep independent client suppression/server-write kills. Disabling one component is a reported degraded combined release, not a successful launch. Rollback preserves all aggregate daily history and previous public assets. |

The final delivery identity is recorded separately in `C:/DevCache/maimai-architecture-delivery/20260922/delivery-receipt.json`: scoped registry/report commits, exact Git archive contract checks, wheel/source-distribution identities and exclusions. A reference to that receipt is not a claim that an earlier test copy already contained the final commit.

No merge, push, deployment, database provisioning, remote report query, protected credential operation, private-report access, new schedule or automation-state change is evidenced by these local validation results. Remaining coverage gaps, platform-specific skipped tests and production-only checks remain visible rather than being represented as completed scenarios.
